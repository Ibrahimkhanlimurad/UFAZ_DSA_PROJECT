#ifndef _MORSE_H_  // prevent recursive inclusion
#define _MORSE_H_
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* morseEncodeChar(int chNum);
char* morseGenerator(char* str);

#endif